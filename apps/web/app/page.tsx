import { CourseCard } from "@repo/ui/components/shared/course-card.tsx";

export default function Page() {
  return (
    <main className="flex items-center justify-center min-h-screen">
      <CourseCard />
    </main>
  );
}
